{
  "env": "production",
  "database": {
    "url": "${DATABASE_URL}"
  },
  "redis": {
    "url": "${REDIS_URL}"
  },
  "logging": {
    "enableSlowQueryLogging": false
  },
  "cors": {
    "allowedOrigins": ["https://app.example.com"]
  }
}
